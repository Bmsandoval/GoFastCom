# run every 5 minutes. run the command at a random interval between now and 150 seconds later
*/5 * * * * sleep $(( RANDOM \% 150 )); cd /gofastcom; PATH=$PATH:/usr/local/opt/go/libexec/bin; go run . record  > /proc/1/fd/1 2>/proc/1/fd/2
